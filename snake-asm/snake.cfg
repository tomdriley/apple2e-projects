# ld65 config for SNAKE.BIN
# Produces a raw binary whose first byte loads at $0800.
# BSAVE/BRUN it at A$0800 on a DOS 3.3 disk.

MEMORY {
    RAM: start = $0800, size = $7800, file = %O;
}

SEGMENTS {
    CODE: load = RAM, type = ro;
}
